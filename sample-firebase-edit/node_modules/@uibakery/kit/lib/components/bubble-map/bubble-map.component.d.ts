import { HttpClient } from '@angular/common/http';
import { ChangeDetectorRef, OnDestroy, OnInit } from '@angular/core';
import { NbThemeService } from '@nebular/theme';
export declare class BkBubbleMapComponent implements OnInit, OnDestroy {
    private theme;
    private http;
    private cd;
    data: {
        countryCoordinates: {};
        mapData: any[];
    };
    options: any;
    private _data;
    private colorConfig;
    private geoColors;
    private countryColorMap;
    private max;
    private initialized;
    private destroyed$;
    constructor(theme: NbThemeService, http: HttpClient, cd: ChangeDetectorRef);
    ngOnInit(): void;
    ngOnDestroy(): void;
    private initColors;
    private updateOptions;
    private getCountryColor;
    private getRandomGeoColor;
}
